import { createTheme } from "@mui/material"

const theme = createTheme({
  palette: {
    primary: {
      main: "#CF132B",
      light: "FFFFFF",
      dark: "#00214D",
    },
  },
})

export default theme
